# JSServer

# Спустя много дней
# Порос костялми код
# Рефакторинг ждёт

## Напоминалка
screen -m -d node index.js // Запуск
##
ps -ef | grep // Поиск
##
tmux new-session -s name
##
tmux attach -t name
